QINGCHENG_API_KEY=
QINGCHENG_BASE_URL=https://aiping.cn/api/v1
QINGCHENG_VISION_MODEL=Qwen2.5-VL-32B-Instruct
QINGCHENG_TEXT_MODEL=DeepSeek-V3.1

# Aholo Labs — World 3DGS + Lux3D 物体生成（Authorization 无 Bearer）
# Vercel: Production + Preview + Development 都要勾上，保存后 Redeploy。
AHOLO_API_KEY=
AHOLO_REGION=cn

# Optional: public URL of a compressed 3DGS (PLZ / SPZ / SOG) for VirtuPath
# Do not point this at the raw 3.29GB Gaussian PLY — the browser cannot decode it at full quality.
EXAMPLE_SPLAT_URL=
